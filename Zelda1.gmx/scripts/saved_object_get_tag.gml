///saved_object_get_tag()
//Returns the unique tag for the given object, used to find any saved data that may exist.
return(room_get_name(room)+string(object_get_name(object_index))+string(x)+string(y));